version: '3'
services:
  speedtest:
    entrypoint: python
    command: ./test-speed.py -g --debug
    build:
      context: .
      dockerfile: Dockerfile-speed

  webgui:
    entrypoint: python
    command: ./graph-web.py
    build:
      context: .
      dockerfile: Dockerfile-web
    ports:
     - "5000:5000"


  speedsql:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: speed_user
      POSTGRES_PASSWORD: speed_pass

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080